# Add this entry to the existing models.yaml in
# Base_Model_BR_PPO/models.yaml — alongside model_a and model_b.
#
# Don't replace the file — copy these 9 lines and paste them under
# the existing model_b entry. Keep the same indentation.

  - id: model_c
    name: "MLP_alpha 130/30 (v3 LLM-augmented features)"
    description: "Sector-neutral 130/30 from quarterly_pipeline_llm. Test Sharpe 2.18 (146d OOS)."
    repo: "FinTechEntrepreneurldz/Model_C_Paper_Trading"
    logs_path: "logs"
    branch: "main"
    enabled: true
    color: "#a855f7"
